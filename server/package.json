{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "postinstall": "chmod +x ./node_modules/.bin/json-server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node node_modules/json-server/lib/cli/bin.js --watch db.json --port $PORT"
},
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
